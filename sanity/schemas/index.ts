import project from './project-schema';
import page from './page-schema';

const schemas = [project, page];
// We did it this way so that when we create new schemas
// we can add them here

export default schemas;